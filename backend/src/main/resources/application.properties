spring.application.name=backend
# 传递配置信息
# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/bookstore?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=czm20041229
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
# 自动更行数据库的表
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# 显示sql语句 格式化
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.devtools.restart.enabled=true
spring.devtools.additional-paths=src/main/java

logging.level.org.springframework.security=DEBUG

# Server Configuration  web应用端口
server.port=8080
# Spring Boot 默认将 Tomcat 作为嵌入式服务器（无需单独安装 Tomcat），打包成可执行的 JAR 文件时直接运行。